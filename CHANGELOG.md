# 变更日志

本文记录言窗稳定版本的重要变化。版本遵循语义化版本。

## 未发布

当前没有未发布的公开变更。

## 1.0.0 - 2026-07-18

### 新增

- 完整中文桌面 API：应用、多窗口、五类布局、十五类控件、画布、定时器、剪贴板和
  文件对话框；
- 固定结构的`事件`对象、自定义事件详情和逐项 VM 回调泵送；
- 布局显式关闭、应用资源快照和公开`错误详情`；
- Windows、macOS、Linux 的 x86-64/ARM64 六目标制品装配流程；
- 三份路径依赖公共消费规格，共 38 个断言。

### 改变

- 稳定版本改为 1.0.0，最低言序提升至 1.1.12，消费范围改为 `^1.0`；
- 配置改为严格类型和范围校验，窗口尺寸、控件值、布局对齐与计时器不再静默修正无效
  输入；
- 关闭窗口的默认行为会清理未处理的窗口子树；绑定关闭处理器时由应用显式决定关闭；
- 原生句柄严格绑定创建线程和宿主事件循环。

### 修复

- 区分借用与自有原生资源描述符，防止释放宿主拥有的句柄；
- 在读取宿主 ABI 表后续字段前验证完整结构大小；
- 模型锁中毒和 Rust panic 均被隔离，同时保留幂等资源清理；
- 取消定时器立即释放保留回调，事件投递逐项泵送；
- 自定义载荷不能覆盖可信事件类型和目标；
- 已关闭资源稳定返回 `GUI_RESOURCE_CLOSED`，不再误报类型错误。

### 安全

- ABI 嵌套值采用聚合元素和总字节预算；
- GUI 资源图、事件绑定、图片尺寸/分配和画布命令/内存均有硬上限；
- 跨线程、跨事件循环和关闭后资源访问被拒绝；
- 原生制品由目标、大小、SHA-256 和 ABI v2 导出共同约束；
- Wayland scanner 使用修复已知 quick-xml 安全公告的锁定补丁。
