引「包:言窗」为 界面；

引「标准:测试」为 测试；

法 空回调（事件）：空 则
    归 空；
终

定 应用 为 界面.应用（「公共 API 测试」）；

测试.相等（应用.调试快照（）【「资源总数」】，1）；

定 窗口 为 应用.窗口（{「标题」：「测试窗口」，「宽」：640，「高」：480，「最小宽」：320，「最小高」：240}）；

定 布局 为 窗口.纵向布局（{「间距」：12，「内边距」：16，「水平对齐」：「中」}）；

定 提示 为 布局.文字（「初始内容」）；

定 输入 为 布局.输入框（{「占位」：「请输入」}）；

定 滑块 为 布局.滑块（0，100，25）；

定 定时 为 应用.定时器（1000，真，空回调）；

提示.内容（「更新内容」）；

提示.工具提示（「说明」）；

提示.可访问名称（「状态提示」）；

输入.内容（「言序」）；

滑块.属性（「值」，75）；

测试.相等（窗口.取标题（），「测试窗口」）；

测试.相等（提示.取内容（），「更新内容」）；

测试.相等（提示.取属性（「工具提示」），「说明」）；

测试.相等（提示.取属性（「可访问名称」），「状态提示」）；

测试.相等（输入.取内容（），「言序」）；

测试.相等（滑块.取属性（「值」），75）；

定 完整快照 为 应用.调试快照（）；

测试.相等（完整快照【「资源总数」】，7）；

测试.相等（完整快照【「应用」】，1）；

测试.相等（完整快照【「窗口」】，1）；

测试.相等（完整快照【「布局」】，1）；

测试.相等（完整快照【「控件」】，3）；

测试.相等（完整快照【「定时器」】，1）；

定时.取消（）；

测试.相等（定时.已取消（），真）；

定时.关闭（）；

测试.相等（应用.调试快照（）【「资源总数」】，6）；

提示.关闭（）；

测试.相等（应用.调试快照（）【「资源总数」】，5）；

布局.关闭（）；

测试.相等（应用.调试快照（）【「资源总数」】，2）；

窗口.关闭（）；

测试.相等（应用.调试快照（）【「资源总数」】，1）；

应用.关闭（）；
