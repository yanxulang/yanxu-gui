引「包:言窗」为 界面；

引「标准:测试」为 测试；

定 应用 为 界面.应用（「事件边界测试」）；

定 窗口 为 应用.窗口（{「标题」：「事件窗口」，「宽」：480，「高」：320}）；

定 按钮 为 窗口.纵向布局（{}）.按钮（「发送」）；

令 收到类型：文 为 「」；

令 收到目标：数 为 0；

令 收到时间：数 为 0；

令 收到详情：典 为 {}；

令 收到处理状态：理 为 假；

法 接收业务事件（所事）：空 则
    置 收到类型 为 所事.类型（）；
    置 收到目标 为 所事.目标（）；
    置 收到时间 为 所事.时间（）；
    置 收到详情 为 所事.详情（）；
    所事.处理（）；
    置 收到处理状态 为 所事.已处理（）；
    归 空；
终

按钮.事件（「业务事件」，接收业务事件）；

定 载荷：典 为 {「类型」：「伪造类型」，「目标」：减 1，「嵌套」：{「状态」：「原始」}}；

按钮.自定义事件（「业务事件」，载荷）；

置 载荷【「嵌套」】【「状态」】 为 「已修改」；

测试.相等（收到类型，「业务事件」）；

测试.断言（（收到目标 大于 0），「自定义事件目标须为真实资源句柄」）；

测试.断言（（收到时间 大于 0），「自定义事件须包含毫秒时间」）；

测试.相等（收到详情【「类型」】，「伪造类型」）；

测试.相等（收到详情【「目标」】，减 1）；

测试.相等（收到详情【「嵌套」】【「状态」】，「原始」）；

测试.相等（收到处理状态，真）；

法 取关闭后错误（）：文 则
    试 则
        按钮.取内容（）；
    救 所误 则
        归 界面.错误详情（所误）【「代码」】；
    终
    归 「未抛错」；
终

按钮.关闭（）；

测试.相等（取关闭后错误（），「GUI_RESOURCE_CLOSED」）；

测试.相等（应用.调试快照（）【「资源总数」】，3）；

窗口.关闭（）；

测试.相等（应用.调试快照（）【「资源总数」】，1）；

应用.关闭（）；
