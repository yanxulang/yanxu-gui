引「包:言窗」为 界面；

引「标准:测试」为 测试；

定 应用 为 界面.应用（「错误边界测试」）；

定 窗口 为 应用.窗口（{「标题」：「有效窗口」，「宽」：640，「高」：480}）；

定 布局 为 窗口.纵向布局（{}）；

法 取错误代码（操作：法）：文 则
    试 则
        操作（）；
    救 所误 则
        归 界面.错误详情（所误）【「代码」】；
    终
    归 「未抛错」；
终

法 建零宽窗口（）：空 则
    应用.窗口（{「宽」：0，「高」：100}）；
    归 空；
终

法 建冲突窗口（）：空 则
    应用.窗口（{「宽」：100，「高」：100，「最小宽」：200}）；
    归 空；
终

法 建错误类型窗口（）：空 则
    应用.窗口（{「宽」：「很宽」，「高」：100}）；
    归 空；
终

法 建错误对齐布局（）：空 则
    窗口.纵向布局（{「水平对齐」：「斜」}）；
    归 空；
终

法 建未支持伸缩布局（）：空 则
    窗口.横向布局（{「伸缩」：1}）；
    归 空；
终

法 建错误滑块（）：空 则
    布局.滑块（10，10，10）；
    归 空；
终

法 建错误进度条（）：空 则
    布局.进度条（2）；
    归 空；
终

法 设错误主题（）：空 则
    应用.主题（「紫外」）；
    归 空；
终

法 建过快定时器（）：空 则
    应用.定时器（1，假，空回调）；
    归 空；
终

法 空回调（事件）：空 则
    归 空；
终

定 文字 为 布局.文字（「尺寸边界」）；

法 设负尺寸（）：空 则
    文字.尺寸（减 1，20）；
    归 空；
终

测试.相等（取错误代码（建零宽窗口），「GUI_SIZE_RANGE」）；

测试.相等（取错误代码（建冲突窗口），「GUI_WINDOW_SIZE」）；

测试.相等（取错误代码（建错误类型窗口），「GUI_VALUE_TYPE」）；

测试.相等（取错误代码（建错误对齐布局），「GUI_LAYOUT_ALIGN」）；

测试.相等（取错误代码（建未支持伸缩布局），「GUI_LAYOUT_FEATURE」）；

测试.相等（取错误代码（建错误滑块），「GUI_CONTROL_RANGE」）；

测试.相等（取错误代码（建错误进度条），「GUI_CONTROL_RANGE」）；

测试.相等（取错误代码（设错误主题），「GUI_THEME」）；

测试.相等（取错误代码（建过快定时器），「GUI_TIMER_RANGE」）；

测试.相等（取错误代码（设负尺寸），「GUI_SIZE_RANGE」）；

应用.关闭（）；
