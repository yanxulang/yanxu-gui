引「包:言窗」为 界面；

定 应用 为 界面.应用（「言序 GUI 示例」）；
定 窗口 为 应用.窗口（{「标题」：「你好，言序」，「宽」：800，「高」：600，「最小宽」：480，「最小高」：320}）；
定 布局 为 窗口.纵向布局（{「间距」：12，「内边距」：16}）；
定 提示 为 布局.文字（「尚未点击」）；
定 输入 为 布局.输入框（{「占位」：「请输入中文、Emoji 或扩展 Unicode」}）；
定 按钮 为 布局.按钮（「点击」）；

法 点击处理（事件）则
    提示.内容（「你好，」加 输入.取内容（））；
终

法 关闭处理（事件）则 应用.退出（）；终

按钮.点击（点击处理）；
窗口.关闭时（关闭处理）；
窗口.显示（）；
应用.运行（）；
