引「包:言窗」为 界面；
定 应用 为 界面.应用（「文件对话框」）；
定 窗口 为 应用.窗口（{「标题」：「文件对话框」，「宽」：560，「高」：260}）；
定 布局 为 窗口.纵向布局（{}）；
定 结果 为 布局.文字（「尚未选择」）；
定 按钮 为 布局.按钮（「打开文件」）；
法 选择（事件）则
    定 路径 为 界面.打开文件（{「标题」：「选择文件」，「过滤」：【{「名称」：「图片」，「扩展名」：【「png」，「jpg」，「jpeg」】}】}）；
    若 路径 等于 空 则 结果.内容（「已取消」）；否则 结果.内容（路径）；终
终
法 关闭处理（事件）则 应用.退出（）；终
按钮.点击（选择）；窗口.关闭时（关闭处理）；窗口.显示（）；应用.运行（）；
