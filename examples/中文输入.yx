引「包:言窗」为 界面；
定 应用 为 界面.应用（「中文输入」）；
定 窗口 为 应用.窗口（{「标题」：「中文 IME 与 Unicode」，「宽」：680，「高」：360}）；
定 布局 为 窗口.纵向布局（{}）；
布局.文字（「请测试拼音／五笔候选框、选择、复制、剪切、粘贴、撤销和重做」）；
定 输入 为 布局.多行输入框（{「占位」：「中文 English 😀 𠀀」}）；
输入.尺寸（620，220）；输入.可访问名称（「中文多行输入」）；
法 关闭处理（事件）则 应用.退出（）；终
窗口.关闭时（关闭处理）；窗口.显示（）；应用.运行（）；
