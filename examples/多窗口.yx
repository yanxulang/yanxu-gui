引「包:言窗」为 界面；
定 应用 为 界面.应用（「多窗口」）；
定 主窗 为 应用.窗口（{「标题」：「主窗口」，「宽」：640，「高」：420}）；
定 副窗 为 应用.窗口（{「标题」：「第二窗口」，「宽」：480，「高」：320}）；
主窗.纵向布局（{}）.文字（「这是主窗口」）；
副窗.纵向布局（{}）.文字（「这是独立的第二个原生窗口」）；
法 关闭处理（事件）则 应用.退出（）；终
主窗.关闭时（关闭处理）；副窗.关闭时（关闭处理）；
主窗.显示（）；副窗.显示（）；应用.运行（）；
