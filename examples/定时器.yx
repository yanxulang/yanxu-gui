引「包:言窗」为 界面；
引「标准:JSON」为 JSON；
定 应用 为 界面.应用（「定时器」）；
定 窗口 为 应用.窗口（{「标题」：「定时器示例」，「宽」：480，「高」：240}）；
定 文字 为 窗口.纵向布局（{}）.文字（「等待定时器……」）；
令 次数 为 0；
法 滴答（事件）则
    置 次数 为 次数 加 1；
    文字.内容（「已触发 」加 JSON.序列化（次数）加「 次」）；
终
法 关闭处理（事件）则 应用.退出（）；终
应用.定时器（1000，真，滴答）；
窗口.关闭时（关闭处理）；窗口.显示（）；应用.运行（）；
