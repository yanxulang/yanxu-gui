引「包:言窗」为 界面；
定 应用 为 界面.应用（「暗色主题」）；应用.主题（「暗色」）；
定 窗口 为 应用.窗口（{「标题」：「暗色主题」，「宽」：640，「高」：420}）；
定 布局 为 窗口.纵向布局（{}）；
布局.文字（「暗色主题、键盘 Tab 导航与高对比度基础适配」）；
布局.按钮（「可聚焦按钮」）.工具提示（「按 Tab 在控件间导航」）；
布局.复选框（「复选项」）；
布局.滑块（0，100，42）；
法 关闭处理（事件）则 应用.退出（）；终
窗口.关闭时（关闭处理）；窗口.显示（）；应用.运行（）；
