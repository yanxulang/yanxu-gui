# 安全政策

## 支持范围

| 版本线 | 状态 |
| --- | --- |
| 1.0.x | 接收安全修复 |
| 0.1.x 及更早 | 不再支持，先升级到 1.0.x |

安全修复会发布新的补丁版本；不会以静默替换既有标签或制品的方式发布。

## 报告漏洞

优先使用 GitHub 仓库的私密漏洞报告或 Security Advisory。若该入口不可用，可发送邮件
至 `xiu@srcmax.com`，主题包含“yanxu-gui security”。请提供：

- 受影响版本、操作系统和架构；
- 最小复现或触发条件；
- 预期影响和是否已公开；
- 已知缓解方式。

在修复和发布补丁前，请勿在公开 Issue 中发布可直接利用的细节、崩溃制品或用户数据。
维护者会先确认收到报告，再依据影响范围协调复现、修复和披露时间。普通功能缺陷请使用
公开 Issue。

## 发布与凭据

仓库、测试夹具和日志不得包含访问令牌、签名私钥、数据库密码或真实用户文件。平台签
名与公证凭据只应存在于发布环境的受保护密钥存储中。Release 制品必须来自锁定依赖的
六目标 CI，并核对大小、SHA-256 和 `yanxu_native_module_v2` 导出。

实现边界、资源限制与非目标见 [docs/SECURITY_MODEL.md](docs/SECURITY_MODEL.md)。
